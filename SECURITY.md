# 公开范围与安全说明

## 本仓库已移除

- `.env` 与所有真实 API Key；
- 仪表盘密码、会话密钥和 Cookie；
- 全量股票日线与指数缓存；
- 本地 SQLite 数据库、运行日志和用户问答历史；
- 虚拟环境、Python 运行时和缓存；
- 含敏感配置的服务器部署包；
- 本地绝对路径和个人身份信息。

## 密钥使用

所有密钥只能写入本地 `.env` 或部署平台的密钥管理服务。`.env.example` 只包含空值和非敏感默认配置。

如发现疑似泄露，请立即撤销对应服务密钥，并通过仓库 Security Advisory 私下报告，不要在公开 Issue 中粘贴密钥。

## 部署要求

- 公网环境必须启用 HTTPS；
- 设置强仪表盘密码与随机会话密钥；
- 设置 `SG_QUANT_COOKIE_SECURE=1`；
- 根据实际额度调整登录和问答限流；
- 不要把调试日志或完整异常正文返回浏览器。

